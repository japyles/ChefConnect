-- 1. Change user_id column in recipes to UUID
ALTER TABLE recipes
ALTER COLUMN user_id TYPE UUID USING user_id::uuid;

-- 2. Add foreign key from recipes.user_id to user_profiles.id
ALTER TABLE recipes
ADD CONSTRAINT fk_recipes_user_id_uuid
FOREIGN KEY (user_id) REFERENCES user_profiles(id);

-- 3. (Optional) If you have data migration needs, please update user_id values to match actual UUIDs from user_profiles.
